### ConnectionRef::execute

Source: https://docs.rs/rusqlite/latest/rusqlite/functions/struct.ConnectionRef.html

Prepares and executes a single SQL statement, returning the number of rows affected.

```APIDOC
## ConnectionRef::execute

### Description
Convenience method to prepare and execute a single SQL statement.
On success, returns the number of rows that were changed or inserted or deleted (via `sqlite3_changes`).

### Method
`execute<P: Params>(&self, sql: &str, params: P) -> Result<usize>`

### Parameters
#### Path Parameters
None

#### Query Parameters
None

#### Request Body
None

### Request Example
#### With positional params
```rust
fn update_rows(conn: &Connection) {
    match conn.execute("UPDATE foo SET bar = 'baz' WHERE qux = ?1", [1i32]) {
        Ok(updated) => println!("{} rows were updated", updated),
        Err(err) => println!("update failed: {}", err),
    }
}
```

#### With positional params of varying types
```rust
fn update_rows(conn: &Connection) {
    match conn.execute(
        "UPDATE foo SET bar = 'baz' WHERE qux = ?1 AND quux = ?2",
        params![1i32, 1.5f64],
    ) {
        Ok(updated) => println!("{} rows were updated", updated),
        Err(err) => println!("update failed: {}", err),
    }
}
```

#### With named params
```rust
fn insert(conn: &Connection) -> Result<usize> {
    conn.execute(
        "INSERT INTO test (name) VALUES (:name)",
        &[(":name", "one")],
    )
}
```

### Response
#### Success Response (200)
- **usize**: The number of rows changed, inserted, or deleted.

### Failure
Will return `Err` if `sql` cannot be converted to a C-compatible string or if the underlying SQLite call fails.
```

--------------------------------

### Execute a single SQL statement with parameters

Source: https://docs.rs/rusqlite/latest/rusqlite/functions/struct.ConnectionRef.html?search=std%3A%3Avec

Use `execute` to prepare and run a single SQL statement. It returns the number of affected rows upon success. This method supports both positional and named parameters for dynamic query construction.

```rust
fn update_rows(conn: &Connection) {
    match conn.execute("UPDATE foo SET bar = 'baz' WHERE qux = ?1", [1i32]) {
        Ok(updated) => println!("{} rows were updated", updated),
        Err(err) => println!("update failed: {}", err),
    }
}
```

```rust
fn update_rows(conn: &Connection) {
    match conn.execute(
        "UPDATE foo SET bar = 'baz' WHERE qux = ?1 AND quux = ?2",
        params![1i32, 1.5f64],
    ) {
        Ok(updated) => println!("{} rows were updated", updated),
        Err(err) => println!("update failed: {}", err),
    }
}
```

```rust
fn insert(conn: &Connection) -> Result<usize> {
    conn.execute(
        "INSERT INTO test (name) VALUES (:name)",
        &[(":name", "one")],
    )
}
```

--------------------------------

### Execute a single SQL statement with varying parameter types

Source: https://docs.rs/rusqlite/latest/rusqlite/functions/struct.ConnectionRef.html

This example demonstrates using `execute` with positional parameters of different data types. The `params!` macro can be used to conveniently construct parameter lists. Ensure SQL validity and type matching.

```rust
fn update_rows(conn: &Connection) {
    match conn.execute(
        "UPDATE foo SET bar = 'baz' WHERE qux = ?1 AND quux = ?2",
        params![1i32, 1.5f64],
    ) {
        Ok(updated) => println!("{} rows were updated", updated),
        Err(err) => println!("update failed: {}", err),
    }
}
```

--------------------------------

### Connection::execute

Source: https://docs.rs/rusqlite/latest/rusqlite/index.html?search=u32+-%3E+bool

Executes a SQL statement that does not return any rows. This is typically used for DDL (Data Definition Language) statements like CREATE TABLE or DML (Data Manipulation Language) statements like INSERT, UPDATE, or DELETE.

```APIDOC
## Connection::execute

### Description
Executes a SQL statement that does not return any rows. This is typically used for DDL (Data Definition Language) statements like CREATE TABLE or DML (Data Manipulation Language) statements like INSERT, UPDATE, or DELETE.

### Method
`conn.execute(sql: &str, params: impl Into<Params>) -> Result<usize>`

### Parameters
- `sql` (string slice): The SQL statement to execute.
- `params` (impl Into<Params>): An object that can be converted into `Params`, representing the parameters for the SQL statement.

### Returns
- `Result<usize>`: A `Result` containing the number of rows affected by the statement if successful, or an `Error` if the execution failed.
```

--------------------------------

### execute

Source: https://docs.rs/rusqlite/latest/rusqlite/struct.CachedStatement.html?search=Option%3CT%3E%2C+%28T+-%3E+U%29+-%3E+Option%3CU%3E

Executes a prepared statement. This method is versatile and can be used for INSERT, UPDATE, DELETE, and other statements that do not return rows. It supports various parameter binding methods.

```APIDOC
## pub fn execute<P: Params>(&mut self, params: P) -> Result<i64>

Executes a prepared statement, returning the number of rows modified.

This function is intended for statements that modify data, such as INSERT, UPDATE, or DELETE. It is possible to misuse this function in a way that it cannot detect, such as by calling it on a statement which _selects_ data rather than modifies it. Please don’t do that.

### Failure

Will return `Err` if binding parameters fails, the executed statement returns rows (in which case `query` should be used instead), or the underlying SQLite call fails.

#### Example: Use with heterogeneous positional parameters
```rust
use rusqlite::{Connection, Result};
fn store_file(conn: &Connection, path: &str, data: &[u8]) -> Result<()> {
    let query = "INSERT OR REPLACE INTO files(path, hash, data) VALUES (?1, ?2, ?3)";
    let mut stmt = conn.prepare_cached(query)?;
    let hash: [u8; 32] = sha256(data);
    // The easiest way to pass positional parameters of have several
    // different types is by using a tuple.
    stmt.execute((path, hash, data))?;
    // Using the `params!` macro also works, and supports longer parameter lists:
    stmt.execute(rusqlite::params![path, hash, data])?;
    Ok(())
}
```

#### Example: Use with named parameters
```rust
fn insert(conn: &Connection) -> Result<()> {
    let mut stmt = conn.prepare("INSERT INTO test (key, value) VALUES (:key, :value)")?;
    // The `rusqlite::named_params!` macro (like `params!`) is useful for heterogeneous
    // sets of parameters (where all parameters are not the same type), or for queries
    // with many (more than 32) statically known parameters.
    stmt.execute(named_params! { ":key": "one", ":val": 2 })?;
    // However, named parameters can also be passed like:
    stmt.execute(&[(":key", "three"), (":val", "four")])?;
    // Or even: (note that a &T is required for the value type, currently)
    stmt.execute(&[(":key", &100), (":val", &200)])?;
    Ok(())
}
```

#### Example: Use without parameters
```rust
fn delete_all(conn: &Connection) -> Result<()> {
    let mut stmt = conn.prepare("DELETE FROM users")?;
    stmt.execute([])?;
    Ok(())
}
```
```

# Lint-check exemption config
# Files/functions listed here are allowed to exceed limits.
# Key: absolute justification required.

[exempt_files]

# Generated from taxonomy.json — cannot split, regenerated from spec
"crates/runie-core/src/event/generated.rs" = "AUTO-GENERATED from taxonomy.json — do not edit manually"

# Keystroke DSL — test infrastructure, complex parsing logic
"crates/runie-testing/src/keystroke_dsl.rs" = "Test keystroke DSL — parser combinators bundled"

# Layout — layout computation
"crates/runie-core/src/layout.rs" = "Layout computation — tightly coupled layout logic"

# Subagents module — subagent management
"crates/runie-core/src/subagents/mod.rs" = "Subagent management module — tightly coupled state"

# Update dispatch — main event routing
"crates/runie-core/src/update/dispatch.rs" = "Update dispatch — event routing hub"

# Agent thought — thought generation
"crates/runie-core/src/update/agent/thought.rs" = "Agent thought processing — thought pipeline"

# Shell — shell integration
"crates/runie-core/src/shell.rs" = "Shell integration — shell command handling"

# Model catalog — model listing
"crates/runie-core/src/model_catalog/mod.rs" = "Model catalog — model listing and filtering"

# Config implementation — config management
"crates/runie-core/src/config/config_impl.rs" = "Config implementation — config management"

# Auth credential — credential handling
"crates/runie-core/src/auth/credential.rs" = "Credential management — tightly coupled to auth"

# Auth storage — key storage
"crates/runie-core/src/auth/storage.rs" = "Key storage — tightly coupled to storage format"

# Provider registry — provider management
"crates/runie-core/src/provider/registry.rs" = "Provider registry — provider discovery"

# Provider detection — provider detection
"crates/runie-core/src/provider/detection.rs" = "Provider detection — detection logic"

# Headless mode — headless TUI
"crates/runie-core/src/headless.rs" = "Headless TUI — headless mode implementation"

# MCP lifecycle — MCP lifecycle management
"crates/runie-core/src/mcp/lifecycle.rs" = "MCP lifecycle management — lifecycle state machine"

# MCP connection — MCP connection handling
"crates/runie-core/src/mcp/connection.rs" = "MCP connection handling — connection management"

# Hooks — pre/post action hooks
"crates/runie-core/src/hooks.rs" = "Pre/post action hooks — hook registration and dispatch"

# Actor input messages — input actor messages
"crates/runie-core/src/actors/input/messages.rs" = "Input actor messages — tightly coupled to input actor"

# Actor provider — provider actor
"crates/runie-core/src/actors/provider/ractor_provider.rs" = "Provider actor — tightly coupled to provider actor"

# Actor turn messages — turn actor messages
"crates/runie-core/src/actors/turn/messages.rs" = "Turn actor messages — tightly coupled to turn actor"

# Actor permission — permission actor
"crates/runie-core/src/actors/permission/ractor_permission.rs" = "Permission actor — tightly coupled to permission actor"

# Actor permission messages — permission messages
"crates/runie-core/src/actors/permission/messages.rs" = "Permission messages — tightly coupled to permission actor"

# Actor turn tests — turn actor tests
"crates/runie-core/src/actors/turn/tests.rs" = "Turn actor tests — integration test suite"

# Durable tests — event durability tests
"crates/runie-core/src/event/durable/tests.rs" = "Event durability tests — test suite"

# Provider tests — provider integration tests
"crates/runie-provider/src/tests.rs" = "Provider integration tests — test suite"

# Session persistence layer — tightly coupled state machine
"crates/runie-core/src/event/durable.rs" = "Event durable serialization — tightly coupled to Event enum"

# Large event module — all Event variants defined here
"crates/runie-core/src/event/mod.rs" = "Canonical Event enum definition site — must reference all variants"

# View rendering — deeply nested render tree, refactor deferred
"crates/runie-core/src/view/elements.rs" = "View element rendering — deep widget tree, refactor deferred"

# Transform utilities — view transformation helpers
"crates/runie-core/src/view/transform.rs" = "View transform utilities — helper functions"

# Stream wrapper — wraps streaming response
"crates/runie-core/src/stream_wrapper.rs" = "Streaming response wrapper — tightly coupled to streaming"

# Terminal backend — PTY/shell interaction, refactor deferred
"crates/runie-core/src/tool/terminal_backend.rs" = "Terminal PTY backend — requires full terminal state"

# Session store — persistence layer, refactor deferred
"crates/runie-core/src/session/store.rs" = "Session store — tightly coupled to session format"

# Session tree — session tree data structure
"crates/runie-core/src/session/tree.rs" = "Session tree — tree data structure"

# Session replay — session replay logic
"crates/runie-core/src/session/replay.rs" = "Session replay — tightly coupled to event stream"

# Session checkpoint — checkpoint serialization
"crates/runie-core/src/session/checkpoint.rs" = "Session checkpoint — tightly coupled to session format"

# Actor session handlers — session actor message handlers
"crates/runie-core/src/actors/session/session_handlers.rs" = "Session actor handlers — tightly coupled to session actor"

# Domain operations — domain model operations
"crates/runie-core/src/model/state/domain_ops.rs" = "Domain model operations — tightly coupled to AppState"

# Turn projections — turn view projections
"crates/runie-core/src/model/state/turn_projections.rs" = "Turn projections — tightly coupled to turn state"

# Skills discovery — skills loading/scanning logic
"crates/runie-core/src/skills/discovery.rs" = "Skills discovery — file system scanning logic"

# Stream response — agent streaming response
"crates/runie-agent/src/stream_response.rs" = "Agent streaming response — tightly coupled to streaming"

# Find definitions — multi-language symbol detection
"crates/runie-agent/src/tool/find_definitions.rs" = "Multi-language symbol detection — language parsers bundled"

# Swarm patterns — pattern orchestration
"crates/runie-patterns/src/swarm.rs" = "Swarm orchestration — pattern composition"

# UI actor — main UI state machine
"crates/runie-tui/src/ui_actor/mod.rs" = "UI actor — main UI state machine, tightly coupled"

# Mouse normalization — input handling
"crates/runie-tui/src/input/mouse_normalize.rs" = "Mouse input normalization — coordinate mapping"

# Pattern runner — pattern execution engine
"crates/runie-tui/src/pattern_runner.rs" = "Pattern runner — pattern execution engine"

# Message support — message rendering utilities
"crates/runie-tui/src/message/support.rs" = "Message rendering support — many render helpers"

# Bootstrap — app initialization
"crates/runie-tui/src/bootstrap.rs" = "App bootstrap — initialization logic"

# Form panel — form rendering
"crates/runie-tui/src/popups/panel/form.rs" = "Form panel — form rendering widgets"

# Mock provider — mock implementation
"crates/runie-provider/src/mock.rs" = "Mock provider — mock implementation for testing"

# Provider replay — provider replay logic
"crates/runie-provider/src/replay.rs" = "Provider replay — fixture replay logic"

# OpenAI stream - SSE parsing
"crates/runie-provider/src/openai/stream.rs" = "OpenAI SSE streaming parser — protocol implementation"

# OpenAI protocol - frame parsing
"crates/runie-provider/src/openai/protocol.rs" = "OpenAI protocol implementation — frame parsing"

# Retry logic - backoff configuration
"crates/runie-provider/src/retry.rs" = "Retry/backoff configuration — configuration struct"

# CLI main - command entry points
"crates/runie-cli/src/main.rs" = "CLI entry point — all commands registered here"

# CLI inspect - inspection subcommands
"crates/runie-cli/src/inspect/mod.rs" = "CLI inspect module — inspection subcommands"

# Agentic loop - main agent orchestration loop
"crates/runie-core/src/agentic_loop.rs" = "Agentic loop — main agent orchestration loop"

# Goal mode — goal pattern orchestration
"crates/runie-core/src/goal.rs" = "Goal pattern — goal mode orchestration pipeline"

# Subagent coordinator — subagent lifecycle management
"crates/runie-agent/src/subagent_coordinator.rs" = "Subagent coordinator — subagent lifecycle management"

# Subagent — subagent implementation
"crates/runie-agent/src/subagent.rs" = "Subagent — subagent implementation"

[exempt_functions]

# Generated event methods — cannot split
"generated.rs" = { "pub fn kind" = "Generated match over Event enum" }
"generated.rs" = { "pub fn category" = "Generated match over Event enum" }
"generated.rs" = { "pub fn into_intent" = "Generated match over Event enum" }

# Dispatch is a large match — refactor deferred
"dispatch.rs" = { "pub fn dispatch" = "Event dispatch match — refactor deferred" }
"dispatch.rs" = { "pub fn describe" = "Action description match" }

# Retry configuration — refactor deferred
"retry.rs" = { "pub fn new" = "Retry configuration builder" }

# View default — view state initialization
"elements.rs" = { "fn default" = "View state default" }

# Question event handler — event processing
"question.rs" = { "pub(crate) fn question_event" = "Question event handler — event processing pipeline" }

# Session event handler — event processing
"session.rs" = { "pub(super) fn handle_session_event" = "Session event handler — event processing" }

# Turn events handler — event routing
"dispatch.rs" = { "fn handle_turn_events" = "Turn events routing — event dispatch hub" }

# Plan mode input — input handling
"update/input/mod.rs" = { "fn plan_mode_input_event" = "Plan mode input — input handling" }

# Vim nav handler — navigation handling
"update/input/nav.rs" = { "pub(crate) fn handle_vim_nav_event" = "Vim navigation handler — navigation state" }

# Command handler — command event processing
"update/command.rs" = { "pub(super) fn handle_command_event" = "Command event handler — command processing" }

# Permission event — permission handling
"update/permission.rs" = { "pub(crate) fn permission_event" = "Permission event handler — permission flow" }

# Model config handler — model configuration
"update/agent/model_config.rs" = { "fn handle_main_events" = "Model config handler — config event routing" }

# Dialog MCP action handler — dialog routing
"update/dialog/router.rs" = { "fn handle_mcp_skill_action" = "MCP skill action handler — dialog routing" }

# Format tool input — tool formatting
"permissions/format.rs" = { "pub fn format_tool_input" = "Tool input formatter — formatting logic" }

# Validate step — step validation
"login_flow/state/mod.rs" = { "pub fn validate_step" = "Login flow step validation" }

# Config handlers — config message handler
"actors/config/handlers.rs" = { "pub(super) async fn handle_msg" = "Config actor message handler" }

# Config messages clone — message cloning
"actors/config/messages.rs" = { "fn clone" = "Config message cloning" }

# Turn messages clone — message cloning
"actors/turn/messages.rs" = { "fn clone" = "Turn message cloning" }

# Permission messages clone — message cloning
"actors/permission/messages.rs" = { "fn clone" = "Permission message cloning" }

# View default — view state
"model/state/view.rs" = { "fn default" = "View state default" }

# Markdown parsing — tag handlers
"markdown/parsing.rs" = { "fn start_tag" = "Markdown tag parser — tag handling" }
"markdown/parsing.rs" = { "fn end_tag" = "Markdown tag parser — end tag handling" }

# HTTP status classification — status code mapping
"provider_trait.rs" = { "pub fn classify_http_status" = "HTTP status classification — status code mapping" }

# Swarm marker response — marker response handling
"mock.rs" = { "fn swarm_marker_response" = "Swarm marker response — marker response handling" }

# Question event — event processing pipeline
"question.rs" = { "pub(crate) fn question_event" = "Question event handler — event processing pipeline" }

# Session event — session event handling
"session.rs" = { "pub(super) fn handle_session_event" = "Session event handler — event processing" }

# Vim nav event — navigation state machine
"nav.rs" = { "pub(crate) fn handle_vim_nav_event" = "Vim navigation handler — navigation state" }

# Command event — command processing
"command.rs" = { "pub(super) fn handle_command_event" = "Command event handler — command processing" }

# Permission event — permission flow
"permission.rs" = { "pub(crate) fn permission_event" = "Permission event handler — permission flow" }

# Tool call parsing — multi-strategy parsing pipeline
"tool/shim/mod.rs" = { "fn parse_line_strategies" = "Tool call parsing — multi-strategy parsing pipeline" }

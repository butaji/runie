# Add `blocked` status and `blocked_reason` fields to tasks

## Status

`todo`

## Description

Promote `blocked` to a first-class task status and require a `blocked_reason` field. Update `tasks/TEMPLATE.md` and `tasks/index.json` schema.

## Acceptance criteria

1. **Unit tests** — N/A; process task.
2. **E2E tests** — N/A; process task.
3. **Live tmux tests** — N/A; process task.

## Tests

### Unit tests
- N/A.

### E2E tests
- N/A.

### Live tmux tests
- N/A.

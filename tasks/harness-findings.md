# Coding harness backlog

Ranked from the current Runie codebase and comparison with `../../kimi-code`.
Status is `planned` until source, event tests, replay tests, and TUI smoke
evidence prove the item.

1. `harness-01-tools` — **partial** — production Read, Write, Edit, Grep, Glob, and Bash tools are registered and tested; centralized traversal/sensitive-path policy now declaratively rejects credential files and sensitive directories, typed bounded read/search, Bash stream summaries, and a shared bounded tool-result terminal projection are covered; public serializable renderer-neutral `ToolCardSummary` and `BackgroundJobSummary` rows now retain resolved tool/job identity, card status, output facts, and ordered aggregate Unicode-safe bounded previews, each owns a stable terminal-row projection, and `/jobs` exposes that preview through a pure terminal row; actor-owned `BackgroundOutput` now carries command, status, exit code, bounded captured text, facts, preview, truncation, and terminal rows through one explicit output-card query, while richer output controls remain.
2. `harness-02-permissions` — **partial** — pure Ask/Auto/YOLO/Deny policy now routes live mutating and unknown tools through either the owned question dialog or an explicit block; one core conversion maps command approval state into tool policy state, while macro-generated approval-mode wire names/reverse parsing and approval commands project into the live mode store; a YAML decision matrix, bounded serializable approval traces, typed allow/deny/ask decision-kind projections, stable lowercase wire-name terminal rows, question-cancellation traces, and reason-preserving terminal decision rows are replayed; richer approval controls remain.
3. `harness-03-shell-lifecycle` — **partial** — Bash now streams stdout/stderr chunks and typed `background_bash`, `background_jobs`, and `background_cancel` cover the owned job lifecycle; background snapshots now bound combined output, preserve failure status/output/exit code, and the core-owned `BackgroundJobSummary::terminal_line` projection drives default and status-filtered `/jobs` rows plus `/jobs <id>` actor-owned bounded output cards carrying lifecycle context; `/jobs cancel <id>`, actor-owned `/jobs cancel all`, actor-owned `/jobs clear finished`, and palette-discoverable completed/failed/cancelled filters remain covered, while richer TUI controls remain.
4. `harness-04-model-effort-ui` — **partial** — model metadata now supplies finite effort rows and selection, `/effort` reopens the current model’s declared picker, and YAML replay coverage proves unsupported levels are excluded; model selection now resets effort through the owning loop actor to the model-declared default when valid, otherwise the first declared level (or off), so cancelling the picker cannot retain an invalid prior-model level; the HTTP request boundary and live MiniMax adapter now consume the model-derived provider profile to preserve each model’s declared provider-wire effort value through a pure, tested request-body projection, MiniMax usage now preserves cache-read/cache-write and reasoning fields, a shared typed effort-map accessor covers all seven levels without inventing unsupported values, and macro-backed typed provider-effort fields plus serializable/replayable OpenAI/Anthropic/Gemini/MiniMax request profiles centralize distinct wire spellings; broader provider-specific payload adapters remain.
5. `harness-05-context` — **partial** — pure token estimation, strict automatic-compaction thresholding, one shared default compaction policy, serde-replayable snake_case compaction decisions/actions, serde-replayable compaction cut points and preparations, actor-owned compaction preparation, typed and serde-replayable `CompactionRecoveryPlan`/`CompactionDecision` and complete `ContextReport` projections (including policy inputs), live `/context` token/window, policy, and threshold rows now consume the pure report, `/context compact [instructions]` control routing through the existing actor pipeline, Kimi-compatible `/clear` and `/reset` actor-owned context reset commands, and a production actor-owned async recovery hook that prepares, summarizes, publishes, and rebuilds context through the shared `SessionActor` now use the active model’s declared context window with a named fallback; required, unknown-window-disabled, and explicitly disabled-policy recovery states now all replay from YAML, while richer compaction controls remain.
6. `harness-06-plan-todos` — **partial** — replayable validated `todo_write` snapshots now reduce through an owned `TodoActor` and explicit serde `TodoEvent`/pure reducer boundary, which also enforces the invariant at its state boundary, including unique stable item identities; `/plan` now has actor-owned `on`, `off`, toggle, `view`, and `clear` transitions, while `/view-plan` projects actor-owned item rows plus the empty/pending/in-progress/complete summary; broader plan enforcement remains.
7. `harness-07-subagents` — **partial** — typed explore/plan/code requests now execute through an owned loop hook, declare replayable role capabilities, reject capability escalation at validation, enforce role-specific serialized output bounds at the executor boundary, return role/capability/output/usage result data, thread cumulative host-reported multi-turn/tool usage through a bounded role-aware execution result, and reduce serializable `started`/`turn`/`completed`/`failed` lifecycle events through one pure role-aware reducer; richer host-side subagent orchestration remains.
8. `harness-08-tool-scheduler` — **partial** — independent read/search tools remain parallel while mutations and subagents are serialized; typed per-tool resource keys now partition only conflicting calls into separate batches, the owned executor mailbox prioritizes interactive work over background work, actor-owned replayable scheduler metrics track queue/running/completed/failed/cancelled work with typed cancellation reasons, separate `cancelled_queued`/`cancelled_running` counters, a serializable `SchedulerMetricRow` projection, macro-generated lowercase cancellation wire names, cancellation-specific terminal rows, running-slot release on cancellation, explicit replay events for queued versus running cancellation, `/jobs scheduler` and `/jobs scheduler active` control projections, and `/jobs running|completed|failed|cancelled` filters actor-owned lifecycle data; richer cancellation controls remain.
9. `harness-09-web` — **partial** — typed bounded `web_search` contract, executor hook, HTTP client with structured citation results, declared/body response-size caps, optional live endpoint wiring, renderer-neutral ranked source cards, and a macro-backed Generic/Brave/Tavily table now generates stable provider wire names plus snippet fields and normalizes the supported adapters; Brave’s nested `web.results` response is normalized alongside its compatibility top-level shape, Tavily’s optional bounded top-level answer is preserved as response data, while additional provider-specific adapters remain.
10. `harness-10-mcp` — **partial** — typed server/tool data, atomic stdio and HTTP discovery/call registration, owned stdio and HTTP actor request mailboxes, protocol-correct initialized notifications, ID-correlated notification-tolerant responses, structured JSON-RPC error propagation, bounded HTTP JSON transport with bearer auth, session-header reuse, explicit async close, a 1 MiB response cap, bounded SSE event projection, bounded notification projection, response/notification reduction with deterministic terminal rows, HTTP `stream_events` delivery, bounded replayable reconnect policy/state and serializable retry/exhaustion decisions, a serializable bounded notification queue with event-driven push/pop/clear controls, ordered event replay, drop accounting, clear now resetting the backpressure epoch, explicit clear/saturated/dropping status, stable backpressure rows, actor-owned ready/busy/failed/closed status for stdio and HTTP, and live unified `/mcps` lifecycle rows with lowercase wire status names plus a core-owned terminal projection and palette-discoverable `stdio`/`http` and ready/busy/failed/closed filters now exist; richer MCP controls remain.
11. `harness-11-plugins` — **partial** — validated declarative plugin manifests, deterministic filesystem package discovery, a duplicate-safe registry, a manifest-only host installer, replayable installation/uninstallation roots, installation-aware active-capability projection, validated load/unload/failure transitions, package-bounded execution, a typed command/tool/hook-to-entrypoint ABI, a timeout/argument/output-capped host runner, deterministic manifest-driven `PluginTool` registration for normal `ToolRegistry` execution, immutable shared runtime projections, macro-backed capability-directory routing, renderer-neutral execution status/stdout/stderr/truncation rows, and stable command/tool/hook capability-name metadata now model extensions; richer metadata remains.
12. `harness-12-git` — **partial** — bounded read-only Git inspection, palette-discoverable `/git status`, `/git diff`, `/git review`, and `/git worktrees`, typed non-mutating `git_commit_prepare`, approval-gated `git_commit`, explicit validated `git_push`, safe inverse-commit `git_revert`, a lossless conflict summary projection with renderer-neutral terminal rows, pure action/path admission, event-reduced recovery, and stable terminal rows for recovery/conflicts now exist; command execution and richer conflict UI remain.
13. `harness-13-sessions` — **partial** — resume/fork/rename/export foundations, actor-owned undo, bounded deterministic storage discovery, serde-replayable session snapshots and statistics, entry/branch/lane queries, and lossless lane record snapshots/envelopes, newest-first session ordering with path tie-breaking, serializable picker rows retaining source paths, pure path-preserving terminal row projections, an actor-owned queryable row boundary, replayable history rows marking the selected branch while retaining alternate undo targets and explicitly projecting `undoable` only for message records reachable by the undo command, `/sessions [text]` command-result browsing, `/sessions history` actor-owned branch-history rows, `/sessions history <entry-id>` actor-owned history selection, explicit `/sessions pick [text]` picker population, no-argument `/resume` picker population, and a palette-discoverable `/undo` action now exist; richer interactive picker/undo history UX remains.
14. `harness-14-provider-contract` — **partial** — normalized usage across Responses/Chat-style wire shapes including Anthropic cache-read/cache-write aliases, one shared typed provider finish-reason mapper with lossless raw values, response IDs/models, replayable transport/cache options, macro-generated transport/retention/window wire projections, one shared status/header-aware retry predicate used by HTTP and stream failure boundaries, and serializable provider failure classification with macro-generated stable failure-kind wire parsing, bounded raw `Retry-After` guidance, and terminal summaries now exist; replay now preserves raw Chat, Responses incomplete (including missing details), failed Responses, filtered and unknown Chat finish values (unknown values fail closed as errors), MiniMax batch and stream finish values now use the shared mapper, MiniMax batch/SSE failures retain provider status codes, and stream finish values while retaining legacy tool-call reduction semantics, and broader adapter conformance remains.
15. `harness-15-user-questions` — **partial** — structured validation, owned broker, live selector, multi-select answers, YAML replay, explicit broker cancellation, broker-side option validation, optional stable option IDs with deterministic fallback identity, bounded JSONL trace export/restore, session-owner persistence, live TUI forwarding for answered/cancelled/rejected requests, bounded text/outcome-filtered history queries, serializable history rows with a core-owned terminal projection, typed `UserQuestionHistoryPage` metadata (`offset`, `limit`, `has_more`), `/questions [outcome=<kind>] [offset=<n>] [limit=<n>] [text]` paginated command-result browsing, actor-owned `/questions clear` history reset, and optional renderer-neutral `header`/`body` metadata projected by the pure TUI dialog are implemented; richer multi-question review/history interaction remains.
16. `harness-16-media` — **partial** — model-declared text/image/video/audio modalities now expose capability-aware predicates, `ToolRegistry::tools_for_model` filters tools by required modality, typed image/video/audio constructors validate MIME/base64 data with a bounded payload cap, and a macro-generated media wire/capability table drives stable wire-name parsing plus provider-specific single/mixed-turn encoding while preserving order across Pi/OpenAI/Gemini/Anthropic formats; inline and URL Pi media, inline Pi/Gemini audio, OpenAI Chat `audio_url`/`video_url`, OpenAI Responses image detail and inline/URL audio files plus URL images, Anthropic base64 video and remote image URLs, and typed TUI audio projection are now supported, while additional provider-specific media formats remain.
17. `harness-17-ide` — **partial** — replayable typed IDE workspace/document/diagnostic protocol data, JSON-RPC codecs accepting numeric and string IDs, an actor-owned `IdeSnapshot` mailbox, one pure RPC-notification-to-event boundary, a bounded newline JSON-RPC wire buffer with actor-facing frame application, LSP document and `publishDiagnostics` notification conversion, connection lifecycle state, bounded serializable diagnostic rows with severity/location/message terminal projections, and deterministic IDE terminal rows now exist; live socket/editor ownership remains.
18. `harness-18-noninteractive` — **partial** — typed JSONL event encoding/decoding, serde-replayable noninteractive configuration, replay-safe usage events, deterministic completed/aborted/failed exit codes, pure `--jsonl`/explicit approval argument parsing, bounded typed provider-event forwarding, and the `runie --jsonl` loop boundary now exposes selected provider/model/model name/API/context, canonical effort, model default effort, effective compaction settings, model-declared provider-wire effort, and effective approval-mode metadata plus the actor-owned provider lifecycle trace alongside prompt-or-stdin input; richer terminal metadata remains.
19. `harness-19-diagnostics` — **partial** — actor-owned `/doctor` now accepts only typed `inspect`/`fix` actions (unknown actions leave the prior report intact) and projects a serializable report with explicit checks and typed inspect/fix action intent as deterministic command-result rows, a typed `DiagnosticReportRow` projection drives those terminal rows, telemetry exposes a serializable usage summary over ended provider spans with a core-owned terminal-row projection, `/usage` reads that same actor projection, and `DiagnosticBundle` exports/replays combined data with bounded renderer-neutral metric, terminal-row, request-series, and sparkline rows; interactive chart controls remain.
20. `harness-20-tui-polish` — searchable selectors, approvals, tasks, tool cards, and keyboard UX.

Existing foundations are not duplicated here: actors/events, provider registry,
model catalog, replay/session storage, compaction primitives, queues, hooks,
telemetry, pure snapshots, command palette, and TUI smoke infrastructure.

Smoke requirement: `scripts/tmux-command-smoke.sh` covers 67 palette cases,
including parameterized background-job inspection, two
direct command cases, and the Quit lifecycle, for 76 TUI-only cases.
Provider-backed coding prompts remain
environment-dependent and must be recorded separately from local UI evidence.

# Coding harness backlog

Ranked from the current Runie codebase and comparison with `../../kimi-code`.
Status is `planned` until source, event tests, replay tests, and TUI smoke
evidence prove the item.

1. `harness-01-tools` — **partial** — production Read, Write, Edit, Grep, Glob, and Bash tools are registered and tested; centralized traversal/sensitive-path policy and typed bounded read/search plus Bash stream summaries are covered, while richer output and background lifecycle remain.
2. `harness-02-permissions` — **partial** — pure Ask/Auto/YOLO/Deny policy now routes live mutating tools through either the owned question dialog or an explicit block; approval commands project into the live mode store; a YAML decision matrix, bounded serializable approval traces, and question-cancellation traces are replayed; richer approval controls remain.
3. `harness-03-shell-lifecycle` — **partial** — Bash now streams stdout/stderr chunks and typed `background_bash`, `background_jobs`, and `background_cancel` cover the owned job lifecycle; background snapshots now bound combined output, preserve failure status/output/exit code, and `/jobs` exposes the owned snapshot plus `/jobs cancel <id>` through the command-result dialog; richer TUI controls/output cards remain.
4. `harness-04-model-effort-ui` — **partial** — model metadata now supplies finite effort rows and selection, with YAML replay coverage proving unsupported levels are excluded; the HTTP request boundary now exposes each model’s declared provider-wire effort value, and typed provider effort fields cover distinct wire spellings; provider-specific payload adapters remain.
5. `harness-05-context` — **partial** — pure token estimation, strict automatic-compaction thresholding, actor-owned compaction preparation, a typed `CompactionDecision` projection, and an optional actor-owned async recovery hook invoked before provider turns now exist; durable summary UX and a production session-owner hook remain.
6. `harness-06-plan-todos` — **partial** — replayable validated `todo_write` snapshots now reduce through an owned `TodoActor`, which also enforces the invariant at its state boundary; a typed empty/pending/in-progress/complete plan summary is available; broader plan enforcement remains.
7. `harness-07-subagents` — **partial** — typed explore/plan/code requests now execute through an owned loop hook, declare replayable role capabilities, reject capability escalation at validation, enforce role-specific serialized output bounds at the executor boundary, return role/capability/output/usage result data, and account for the owning execution turn/tool call; multi-turn runtime accounting remains.
8. `harness-08-tool-scheduler` — **partial** — independent read/search tools remain parallel while mutations and subagents are serialized; typed per-tool resource keys now partition only conflicting calls into separate batches, the owned executor mailbox prioritizes interactive work over background work, and replayable scheduler metrics track queue/running/completed/failed/cancelled work with typed cancellation reasons, including running-slot release on cancellation; richer cancellation UI remains.
9. `harness-09-web` — **partial** — typed bounded `web_search` contract, executor hook, HTTP client with structured citation results, declared/body response-size caps, optional live endpoint wiring, and renderer-neutral ranked source cards now exist; provider-specific adapters remain.
10. `harness-10-mcp` — **partial** — typed server/tool data, atomic stdio discovery/call registration, protocol-correct initialized notifications, ID-correlated notification-tolerant responses, structured JSON-RPC error propagation, bounded HTTP JSON transport with bearer auth, session-header reuse, explicit async close, a 1 MiB response cap, bounded SSE event projection, bounded notification projection, response/notification reduction, HTTP `stream_events` delivery, bounded replayable reconnect policy/state, and a serializable bounded notification queue with drop accounting now exist; transport-owned backpressure UX remains.
11. `harness-11-plugins` — **partial** — validated declarative plugin manifests, deterministic filesystem package discovery, a duplicate-safe registry, replayable installation/uninstallation roots, installation-aware active-capability projection, and replayable load/unload/failure runtime state now model commands, tools, and hooks; host installation I/O and execution remain.
12. `harness-12-git` — **partial** — bounded read-only Git inspection, typed non-mutating `git_commit_prepare`, approval-gated `git_commit`, explicit validated `git_push`, safe inverse-commit `git_revert`, a lossless conflict summary projection, pure action/path admission, and an event-reduced interactive recovery state now exist; command execution and richer conflict UI remain.
13. `harness-13-sessions` — **partial** — resume/fork/rename/export foundations, actor-owned undo, bounded deterministic storage discovery, serializable picker rows, an actor-owned queryable row boundary, and `/sessions` command-result browsing now exist; richer interactive picker/undo history UX remains.
14. `harness-14-provider-contract` — **partial** — normalized usage across Responses/Chat-style wire shapes, typed finish reasons with lossless raw provider values, response IDs/models, replayable transport/cache options, retry policy, and serializable provider failure classification now exist; broader adapter conformance remains.
15. `harness-15-user-questions` — **partial** — structured validation, owned broker, live selector, multi-select answers, YAML replay, explicit broker cancellation, broker-side option validation, bounded JSONL trace export/restore, session-owner persistence, live TUI forwarding for answered/cancelled/rejected requests, bounded text/outcome-filtered history queries, and serializable history rows are implemented; richer history interaction remains.
16. `harness-16-media` — **partial** — model-declared text/image/video modalities now expose capability-aware predicates, `ToolRegistry::tools_for_model` filters tools by required modality, typed image/video constructors validate MIME/base64 data with a bounded payload cap, and provider-specific single/mixed-turn media encoding now preserves order; provider-specific video wire formats remain.
17. `harness-17-ide` — **partial** — replayable typed IDE workspace/document/diagnostic protocol data, typed JSON-RPC codecs, an LSP/ACP notification-to-event bridge, and connection lifecycle state now exist; live socket/editor ownership remains.
18. `harness-18-noninteractive` — **partial** — typed JSONL event encoding/decoding, deterministic completed/aborted/failed exit codes, pure `--jsonl`/explicit approval argument parsing, bounded typed provider-event forwarding, and the `runie --jsonl` loop boundary with prompt-or-stdin input now exist; richer provider lifecycle forwarding remains.
19. `harness-19-diagnostics` — **partial** — actor-owned `/doctor` now projects a serializable report with explicit checks and fix intent, telemetry exposes a serializable usage summary over ended provider spans, and `DiagnosticBundle` exports/replays combined data with bounded renderer-neutral metric, terminal-row, request-series, and sparkline projections; interactive chart controls remain.
20. `harness-20-tui-polish` — searchable selectors, approvals, tasks, tool cards, and keyboard UX.

Existing foundations are not duplicated here: actors/events, provider registry,
model catalog, replay/session storage, compaction primitives, queues, hooks,
telemetry, pure snapshots, command palette, and TUI smoke infrastructure.

Smoke requirement: `scripts/tmux-command-smoke.sh` covers 50 palette cases plus
the Quit lifecycle, for 51 TUI-only cases. Provider-backed coding prompts remain
environment-dependent and must be recorded separately from local UI evidence.

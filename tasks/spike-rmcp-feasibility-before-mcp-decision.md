# Spike `rmcp` feasibility before MCP decision

## Status

`todo`

## Description

Time-boxed spike to evaluate whether `rmcp` can replace the custom MCP scaffolding. If feasibility is low, remove MCP config instead.

## Acceptance criteria

1. **Unit tests** — Spike branch demonstrates a working `rmcp` client call to a local MCP server, or documents why it fails.
2. **E2E tests** — N/A; spike.
3. **Live tmux tests** — N/A; spike.

## Tests

### Unit tests
- `rmcp` client call works in a spike test.

### E2E tests
- N/A.

### Live tmux tests
- N/A.

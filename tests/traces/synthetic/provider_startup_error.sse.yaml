trace_id: provider_startup_error
conversation: synthetic_provider_errors
turn: 1
outcome: startup_error
transport:
  status: 200
  sse: true
request:
  model: synthetic-error-fixture
provider:
  expected_events: []
payload:
  non_empty_text: false
core:
  required_events: [AgentStart, TurnStart, MessageStart, MessageEnd, AgentEnd]
  exact_events: [AgentStart, TurnStart, MessageStart, MessageEnd, MessageStart, MessageEnd, TurnEnd, AgentEnd]
state:
  assistant_messages: 1
  is_streaming: false
  pending_tool_calls: 0
  error_message: "api: upstream unavailable"
tools: []
error:
  kind: startup
  message_contains: "api: upstream unavailable"
description: |
  Synthetic provider startup failure. The harness runs the real loop actor
  with a deterministic failing provider and checks pi-compatible metadata and
  terminal event ordering.

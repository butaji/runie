{
  "id": "file_stale_edit",
  "name": "File Modified During Edit Detection",
  "description": "Test that when a file is modified between read and write operations (TOCTOU race), the tool returns a meaningful error instead of silently overwriting changes. Implement mtime comparison: store mtime on read, verify on write, return StaleEdit error if mismatch.",
  "setup": {
    "files": {}
  },
  "expected": {
    "has_mtime_tracking": true,
    "detects_file_change": true,
    "returns_meaningful_error": true,
    "does_not_silently_overwrite": true
  },
  "grader": "grader.py"
}

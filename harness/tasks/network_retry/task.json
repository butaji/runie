{
  "id": "network_retry",
  "name": "Network Transient Error Retry",
  "description": "Test that tools implement retry logic for transient network errors (timeouts, connection reset, rate limits). Implement exponential backoff retry with configurable max attempts. Transient errors should not immediately fail the agent.",
  "setup": {
    "files": {}
  },
  "expected": {
    "has_retry_wrapper": true,
    "has_exponential_backoff": true,
    "detects_transient_errors": true,
    "has_max_retries": true
  },
  "grader": "grader.py"
}

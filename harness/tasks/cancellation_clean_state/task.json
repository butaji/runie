{
  "id": "cancellation_clean_state",
  "name": "Clean State After Cancellation",
  "description": "Test that when the user cancels an agent (Ctrl+C or Cmd::Interrupt), the system: 1) aborts the running task, 2) resets agent_running to false, 3) returns to Chat mode, 4) preserves workspace integrity (no partial file edits), and 5) allows new submissions.",
  "setup": {
    "files": {
      "src/lib.rs": "//! Module demonstrating potential partial edit scenario.\npub fn process_data(input: &str) -> String {\n    format!(\"Processed: {}\", input)\n}\n"
    }
  },
  "expected": {
    "task_aborted": true,
    "agent_running_reset": true,
    "mode_returns_to_chat": true,
    "workspace_preserved": true,
    "new_submit_allowed": true
  },
}

{
  "id": "stream_error_partial_response",
  "name": "Stream Error Partial Response Display",
  "description": "Test that when a streaming error occurs mid-stream (network drop, model error, invalid token), the system: 1) sends an error event to the UI, 2) displays partial content received before the error, 3) marks the message appropriately, and 4) allows the user to continue.",
  "setup": {
    "files": {
      "examples/stream_demo.rs": "// Example showing stream processing.\nasync fn process_stream(stream: Stream) -> String {\n    let mut result = String::new();\n    while let Some(chunk) = stream.next().await {\n        result.push_str(&chunk);\n    }\n    result\n}\n"
    }
  },
  "expected": {
    "error_event_sent": true,
    "partial_content_displayed": true,
    "message_marked_error": true,
    "user_can_continue": true
  },
  "grader": "grader.py"
}

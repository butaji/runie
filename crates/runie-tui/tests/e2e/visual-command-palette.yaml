name: visual-command-palette
description: Grok command palette opens, filters entries, and preserves actor-owned query state.
events: []
assertions:
  visual:
    cols: 80
    rows: 24
    steps:
      - Ctrl+P
      - "new"
      - Esc
    ui:
      command_palette_open: true
      command_palette_query: ""
      command_palette_index: 0
    screen_text:
      - "Commands"
      - "New Session"

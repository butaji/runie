name: visual-minimal
description: Welcome surface at idle mirrors Grok's full-mode chrome.
events: []
assertions:
  visual:
    cols: 80
    rows: 24
    screen_text:
      - "New worktree"
      - "Resume session"
      - "Workflows are here!"
      - "Ctrl+Enter"
      - "❯"
      - "Enter:send"
    screen_excludes:
      - "panicked"

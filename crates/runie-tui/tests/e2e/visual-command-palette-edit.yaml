name: visual-command-palette-edit
description: Palette query editing remains declarative and actor-owned.
events: []
assertions:
  visual:
    cols: 80
    rows: 24
    steps:
      - Ctrl+P
      - "newx"
      - Backspace
    ui:
      command_palette_open: true
      command_palette_query: "new"
      command_palette_index: 0
    screen_text:
      - "Commands"
      - "New Session"

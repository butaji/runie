name: visual-slash-scoped-models
description: Pi's /scoped-models command opens the actor-owned scoped selector.
events: []
assertions:
  visual:
    cols: 80
    rows: 24
    steps:
      - SeedModelCatalog
      - "/scoped-models"
      - Enter
    ui:
      model_selector_open: true
      model_selector_scoped_only: true
      model_selector_index: 0
    screen_text:
      - "Models"
      - "xai/grok-4"

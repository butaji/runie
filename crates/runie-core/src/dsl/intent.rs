//! Intent types for the declarative DSL.
//!
//! Re-exports the unified `Intent` enum from `event/intent.rs` so the DSL
//! has a single canonical import point.

pub use crate::event::intent::Intent;

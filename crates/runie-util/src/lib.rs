//! Runie utility crate — shared helpers for the runie workspace.
//!
//! This crate contains small, general-purpose utilities that are used across
//! multiple crates but don't belong in `runie-core` (which is domain-specific).

pub mod display_width;
pub mod labels;

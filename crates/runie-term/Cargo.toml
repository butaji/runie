# Archived: all sources moved to runie-tui.
# This crate re-exports runie-tui for backwards compatibility.
# Will be removed in a future release.

[package]
name = "runie-term"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
runie-tui = { path = "../runie-tui" }
